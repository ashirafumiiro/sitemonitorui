import React, {Component} from 'react';

export default class Devices extends Component{
    render() {
        return (
            <div>
                <h2>Devices</h2>
            </div>
        );
    }
}